# Produção / Railway — porta vem do Procfile (--server.port=$PORT)
[server]
headless = true
address = "0.0.0.0"
enableCORS = true
enableXsrfProtection = true

[browser]
gatherUsageStats = false
